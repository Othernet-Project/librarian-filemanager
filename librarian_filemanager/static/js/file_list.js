// Generated by CoffeeScript 1.10.0
(function(window, $) {
  'use strict';
  var DOWN, UP, body, fileList, mainPanel, modalDialogTemplate;
  UP = 38;
  DOWN = 40;
  fileList = $('#file-list');
  body = $(document.body);
  mainPanel = $("#" + window.o.pageVars.mainPanelId);
  modalDialogTemplate = window.templates.modalDialogCancelOnly;
  mainPanel.on('keydown', '.file-list-link', function(e) {
    var elem, listItem;
    elem = $(this);
    listItem = elem.parents('.file-list-item');
    switch (e.which) {
      case UP:
        listItem.prev().find('a').focus();
        e.preventDefault();
        break;
      case DOWN:
        listItem.next().find('a').focus();
        e.preventDefault();
    }
  });
  return mainPanel.on('click', function(e) {
    var elem, openerUrl;
    elem = $(this);
    openerUrl = elem.data('opener-url');
    if (openerUrl == null) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    $.modalContent(openerUrl, {
      successTemplate: modalDialogTemplate
    });
  });
})(this, this.jQuery);
