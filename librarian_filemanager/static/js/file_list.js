// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var DOWN, UP, body, fileList;
  UP = 38;
  DOWN = 40;
  fileList = $('#file-list');
  body = $(document.body);
  return body.on('keydown', '.file-list-link', function(e) {
    var elem, listItem;
    elem = $(this);
    listItem = elem.parents('.file-list-item');
    switch (e.which) {
      case UP:
        listItem.prev().find('a').focus();
        return e.preventDefault();
      case DOWN:
        listItem.next().find('a').focus();
        return e.preventDefault();
    }
  });
})(this, this.jQuery);
