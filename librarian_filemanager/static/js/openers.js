// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var AUTOCLICK_DELAY, genericOpener, modalOverlay, openers, sendOpenerEvent;
  AUTOCLICK_DELAY = 1500;
  openers = $('.opener a');
  modalOverlay = openers.parents('.o-modal-overlay');
  genericOpener = $('.opener-generic');
  sendOpenerEvent = function(opener) {
    var path, type;
    path = opener.data('path');
    type = opener.data('type');
    ($(window)).trigger('opener-click', [
      {
        path: path,
        type: type
      }
    ]);
  };
  openers.on('click', function() {
    var el;
    modalOverlay.trigger('click');
    el = $(this);
    sendOpenerEvent(el);
  });
  if (openers.length === 1) {
    genericOpener[0].click();
  }
})(this, this.jQuery);
