// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var AUTOCLICK_DELAY, closeOverlay, genericOpener, modalOverlay, openers, sendOpenerEvent, url;
  AUTOCLICK_DELAY = 500;
  openers = $('.opener');
  modalOverlay = openers.parents('.o-modal-overlay');
  genericOpener = $('.opener-generic');
  closeOverlay = function() {
    return modalOverlay.trigger('click');
  };
  sendOpenerEvent = function(opener) {
    var path, type;
    path = opener.data('path');
    type = opener.data('type');
    return ($(window)).trigger('opener-click', [
      {
        path: path,
        type: type
      }
    ]);
  };
  openers.on('click', function() {
    var el;
    el = $(this);
    return sendOpenerEvent(el);
  });
  if (openers.length === 1) {
    url = genericOpener.attr('href');
    sendOpenerEvent(genericOpener);
    closeOverlay();
    return setTimeout(function() {
      return window.location = url;
    }, AUTOCLICK_DELAY);
  } else {

  }
})(this, this.jQuery);
