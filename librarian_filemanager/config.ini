[database]
names =
    files

[files]
rootdir = /

[templates]
directory = views

[assets]
directory = static

js_bundles =
    filemanager: file_list

css_bundles =
    filemanager: file_list, opener_list, opener_detail, remove_confirm
